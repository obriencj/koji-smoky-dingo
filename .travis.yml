

os: linux

dist: xenial

language: python

python:
  - "3.6"
  - "3.7"
  - "3.8"
  - "3.9"

cache: pip

install:
  - pip install tox-travis coveralls

script:
  - tox

after_success:
  - if [ "$TRAVIS_PYTHON_VERSION" = "3.8" ] ; then coveralls ; fi


# The end.
